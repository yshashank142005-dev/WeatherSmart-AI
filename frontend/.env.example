# VITE_API_URL — set this to your Render backend URL for production builds.
# Leave it empty (or remove this file) for local development; the Vite dev proxy handles /api.
#
# Example:
#   VITE_API_URL=https://weathersmart-ai-backend.onrender.com
VITE_API_URL=
